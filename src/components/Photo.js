import { react } from "@babel/types";
import React from 'react';

const Photo = () => {
    return(
        <div className="container">
               <h1>
                   Photo
               </h1>
        </div>
    )
}

export default Photo;

